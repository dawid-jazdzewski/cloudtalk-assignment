import { appConfig } from './app';
import { authConfig } from './auth';
import { databaseConfig } from './database';

export { appConfig, authConfig, databaseConfig };

export const configurationFiles = [appConfig, authConfig, databaseConfig];
